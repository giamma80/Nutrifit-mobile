## Empty placeholder workflow (deprecated)
## Tutte le logiche CI specifiche sono distribuite nei workflow dedicati:
## - backend-preflight.yml
## - backend-ci.yml
## - mobile-ci.yml
## Questo file rimane per evitare rotture di riferimenti esterni. Può essere rimosso in futuro.
name: deprecated-placeholder-ci

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo 'Placeholder workflow: nessuna azione.'
